import React, { Component } from 'react';

class Footer extends Component {
    render() {
        return (
            <footer>
                Some footer text
            </footer>
        );
    }
}

export default Footer;
